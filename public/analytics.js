export function totals(rows){let expense=0,income=0,debt=0,investment=0;const categories={};for(const t of rows){if(['expense','refund'].includes(t.kind)){expense-=t.amount;categories[t.category]=(categories[t.category]||0)-t.amount}if(t.kind==='income')income+=t.amount;if(t.kind==='debt')debt-=t.amount;if(t.kind==='investment')investment-=t.amount}return {expense,income,debt,investment,cashflow:income-expense-debt,savingsRate:income?(income-expense-debt)/income:null,categories}}
export function recurring(rows){const groups={};for(const t of rows.filter(x=>x.kind==='expense')){let name=t.description.toUpperCase();if(/PLUS/.test(name))name='Яндекс Плюс · '+(-t.amount/100)+' ₽';else if(/UCA NETWORK/.test(name))name='Домашний интернет';else if(/МТС/.test(name))name='МТС';else if(/PREMIER/.test(name))name='Premier · проверить пробный период';else if(/АЕЗА|SG\*PREMIUM|KINOPOISK/.test(name))name=name.replace(/ОПЛАТА.*?УСЛУГ |ОПЛАТА СБП QR /,'').split(' в ')[0];else continue;(groups[name]??=[]).push(t)}return Object.entries(groups).map(([name,items])=>({name,items,total:-items.reduce((s,t)=>s+t.amount,0),confirmed:new Set(items.map(t=>t.date.slice(0,7))).size>=2}))}
